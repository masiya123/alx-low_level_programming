#include <stdio.h>
/**
 *  main - prog Entry point
 *  Return: 0 Always (Success)
 */

int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
