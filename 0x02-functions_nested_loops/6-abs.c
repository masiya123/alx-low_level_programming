#include "main.h"

/**
 * _abs - comuters the absolute value of an integer
 * @n: the int to check
 * Return: the absolute value or int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
